# 앱 시작 시 push_pwa_app_be/.env 를 자동 로드합니다. (venv와 무관)
# 이 파일을 복사해 .env 로 저장한 뒤 경로를 수정하세요.

# Firebase Admin SDK - 서비스 계정 JSON 절대 경로 권장 (실행 위치와 무관)
# 예: GOOGLE_APPLICATION_CREDENTIALS=C:\Users\...\push_pwa_app_be\keys\serviceAccountKey.json
# 상대 경로도 가능: keys/serviceAccountKey.json (main.py 기준)
GOOGLE_APPLICATION_CREDENTIALS=

# 또는 동일 용도
# FIREBASE_SERVICE_ACCOUNT_PATH=keys/serviceAccountKey.json

# (선택) 설정 시 POST /api/send-push 요청에 X-API-Key 헤더 필요. 비우면 인증 없음.
# PUSH_API_KEY=
